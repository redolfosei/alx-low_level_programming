#include "main.h"

/**
* mul - multiplies two integers and return the results
* @a: first integer to multiply the other
* @b: second integer to multiply the first one
*
* Return: Returns the results of a * b
*/


int mul(int a, int b)
{
return (a * b);
}
