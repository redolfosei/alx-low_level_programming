#include "main.h"
#include <string.h>

/**
 **_strcpy - copy strings
 *@dest: is the destin
 *@src: is the source
 * Return: return a value
 */

char *_strcpy(char *dest, char *src)
{
return (strcpy(dest, src));
}

